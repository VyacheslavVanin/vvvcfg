/*
 *  This file was generated by cfsmgen script.
 *  See www.github.com/VyacheslavVanin/cfmgen.git 
 */

#include "dncfg_token_fsm.h"

const char* dncfg_token_state_names[] = {
    "BEGIN",
    "BEGIN_SPACE",
    "NAME",
    "STRING",
    "NUM",
    "REF_START",
    "SPACE",
    "NEXTLINE",
    "COMMENT",
    "ERROR",
    "REF",
    "STRING_ESC",
    "NUM_DOT",
    "NUM_DOT_NUM"
};

void dncfg_token_step(dncfg_token_ctx_t* ctx)
{
    const DNCFG_TOKEN_STATE state = ctx->state;
    dncfg_token_data_t* data = ctx->data;

    switch(state) {
    case DNCFG_TOKEN_BEGIN: 
        if (dncfg_token_is_space(data)) {
            start_space_token(data);
            append_symbol(data);
            ctx->state = DNCFG_TOKEN_BEGIN_SPACE;
            break;
        }
        if (dncfg_token_is_eol(data)) {
            inc_line_number(data);
            line_count_token(data);
            commit(data);
            break;
        }
        if (dncfg_token_is_alpha(data)) {
            start_name_token(data);
            append_symbol(data);
            ctx->state = DNCFG_TOKEN_NAME;
            break;
        }
        if (dncfg_token_is_quote(data)) {
            start_string_token(data);
            ctx->state = DNCFG_TOKEN_STRING;
            break;
        }
        if (dncfg_token_is_number(data)) {
            start_number_token(data);
            append_symbol(data);
            ctx->state = DNCFG_TOKEN_NUM;
            break;
        }
        if (dncfg_token_is_dollar(data)) {
            start_ref_token(data);
            ctx->state = DNCFG_TOKEN_REF_START;
            break;
        }
        if (dncfg_token_is_comma(data)) {
            start_comma(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_eq(data)) {
            start_eq(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_backslash(data)) {
            ctx->state = DNCFG_TOKEN_NEXTLINE;
            break;
        }
        if (dncfg_token_is_sharp(data)) {
            ctx->state = DNCFG_TOKEN_COMMENT;
            break;
        }
    break;
    case DNCFG_TOKEN_BEGIN_SPACE: 
        if (dncfg_token_is_eol(data)) {
            inc_line_number(data);
            line_count_token(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_BEGIN;
            break;
        }
        if (dncfg_token_is_space(data)) {
            append_symbol(data);
            break;
        }
        if (dncfg_token_is_alpha(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_quote(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_number(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_dollar(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_comma(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_eq(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_close_square_br(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_open_square_br(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_backslash(data)) {
            ctx->state = DNCFG_TOKEN_NEXTLINE;
            break;
        }
        if (dncfg_token_is_sharp(data)) {
            ctx->state = DNCFG_TOKEN_COMMENT;
            break;
        }
        ctx->state = DNCFG_TOKEN_ERROR;
    break;
    case DNCFG_TOKEN_NAME: 
        if (dncfg_token_is_eol(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_alpha(data)) {
            append_symbol(data);
            break;
        }
        if (dncfg_token_is_number(data)) {
            append_symbol(data);
            break;
        }
        if (dncfg_token_is_space(data)) {
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_comma(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_eq(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_backslash(data)) {
            commit(data);
            ctx->state = DNCFG_TOKEN_NEXTLINE;
            break;
        }
        if (dncfg_token_is_sharp(data)) {
            commit(data);
            ctx->state = DNCFG_TOKEN_COMMENT;
            break;
        }
        on_error(data);
        ctx->state = DNCFG_TOKEN_ERROR;
    break;
    case DNCFG_TOKEN_STRING: 
        if (dncfg_token_is_backslash(data)) {
            ctx->state = DNCFG_TOKEN_STRING_ESC;
            break;
        }
        if (dncfg_token_is_quote(data)) {
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_eol(data)) {
            on_error(data);
            ctx->state = DNCFG_TOKEN_ERROR;
            break;
        }
        append_symbol(data);
    break;
    case DNCFG_TOKEN_NUM: 
        if (dncfg_token_is_eol(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_number(data)) {
            append_symbol(data);
            break;
        }
        if (dncfg_token_is_dot(data)) {
            append_symbol(data);
            ctx->state = DNCFG_TOKEN_NUM_DOT;
            break;
        }
        if (dncfg_token_is_space(data)) {
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_comma(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_eq(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_backslash(data)) {
            commit(data);
            ctx->state = DNCFG_TOKEN_NEXTLINE;
            break;
        }
        if (dncfg_token_is_sharp(data)) {
            commit(data);
            ctx->state = DNCFG_TOKEN_COMMENT;
            break;
        }
        if (dncfg_token_is_close_square_br(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_open_square_br(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        on_error(data);
        ctx->state = DNCFG_TOKEN_ERROR;
    break;
    case DNCFG_TOKEN_REF_START: 
        if (dncfg_token_is_alpha(data)) {
            append_symbol(data);
            ctx->state = DNCFG_TOKEN_REF;
            break;
        }
        on_error(data);
        ctx->state = DNCFG_TOKEN_ERROR;
    break;
    case DNCFG_TOKEN_SPACE: 
        if (dncfg_token_is_eol(data)) {
            inc_line_number(data);
            start_newline_token(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_BEGIN;
            break;
        }
        if (dncfg_token_is_space(data)) {
            break;
        }
        if (dncfg_token_is_alpha(data)) {
            start_name_token(data);
            append_symbol(data);
            ctx->state = DNCFG_TOKEN_NAME;
            break;
        }
        if (dncfg_token_is_quote(data)) {
            start_string_token(data);
            ctx->state = DNCFG_TOKEN_STRING;
            break;
        }
        if (dncfg_token_is_number(data)) {
            start_number_token(data);
            append_symbol(data);
            ctx->state = DNCFG_TOKEN_NUM;
            break;
        }
        if (dncfg_token_is_dollar(data)) {
            start_ref_token(data);
            ctx->state = DNCFG_TOKEN_REF_START;
            break;
        }
        if (dncfg_token_is_comma(data)) {
            start_comma(data);
            commit(data);
            break;
        }
        if (dncfg_token_is_eq(data)) {
            start_eq(data);
            commit(data);
            break;
        }
        if (dncfg_token_is_backslash(data)) {
            ctx->state = DNCFG_TOKEN_NEXTLINE;
            break;
        }
        if (dncfg_token_is_sharp(data)) {
            ctx->state = DNCFG_TOKEN_COMMENT;
            break;
        }
        if (dncfg_token_is_open_square_br(data)) {
            start_open_square_br(data);
            commit(data);
            break;
        }
        if (dncfg_token_is_close_square_br(data)) {
            start_close_square_br(data);
            commit(data);
            break;
        }
        on_error(data);
        unexpected_symbol(data);
        ctx->state = DNCFG_TOKEN_ERROR;
    break;
    case DNCFG_TOKEN_NEXTLINE: 
        if (dncfg_token_is_eol(data)) {
            inc_line_number(data);
            line_count_token(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        unexpected_symbol(data);
        ctx->state = DNCFG_TOKEN_ERROR;
    break;
    case DNCFG_TOKEN_COMMENT: 
        if (dncfg_token_is_eol(data)) {
            inc_line_number(data);
            start_newline_token(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_BEGIN;
            break;
        }
    break;
    case DNCFG_TOKEN_ERROR: 
    break;
    case DNCFG_TOKEN_REF: 
        if (dncfg_token_is_eol(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_alpha(data)) {
            append_symbol(data);
            break;
        }
        if (dncfg_token_is_number(data)) {
            append_symbol(data);
            break;
        }
        if (dncfg_token_is_dot(data)) {
            append_symbol(data);
            ctx->state = DNCFG_TOKEN_REF_START;
            break;
        }
        if (dncfg_token_is_space(data)) {
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_comma(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_eq(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_backslash(data)) {
            commit(data);
            ctx->state = DNCFG_TOKEN_NEXTLINE;
            break;
        }
        if (dncfg_token_is_sharp(data)) {
            commit(data);
            ctx->state = DNCFG_TOKEN_COMMENT;
            break;
        }
        on_error(data);
        ctx->state = DNCFG_TOKEN_ERROR;
    break;
    case DNCFG_TOKEN_STRING_ESC: 
        if (dncfg_token_is_escape(data)) {
            append_escape_symbol(data);
            ctx->state = DNCFG_TOKEN_STRING;
            break;
        }
        if (dncfg_token_is_eol(data)) {
            on_error(data);
            ctx->state = DNCFG_TOKEN_ERROR;
            break;
        }
        ctx->state = DNCFG_TOKEN_STRING;
    break;
    case DNCFG_TOKEN_NUM_DOT: 
        if (dncfg_token_is_eol(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_number(data)) {
            append_symbol(data);
            ctx->state = DNCFG_TOKEN_NUM_DOT_NUM;
            break;
        }
        if (dncfg_token_is_space(data)) {
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_comma(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_eq(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_backslash(data)) {
            commit(data);
            ctx->state = DNCFG_TOKEN_NEXTLINE;
            break;
        }
        if (dncfg_token_is_sharp(data)) {
            commit(data);
            ctx->state = DNCFG_TOKEN_COMMENT;
            break;
        }
        if (dncfg_token_is_close_square_br(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_open_square_br(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        on_error(data);
        ctx->state = DNCFG_TOKEN_ERROR;
    break;
    case DNCFG_TOKEN_NUM_DOT_NUM: 
        if (dncfg_token_is_eol(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_number(data)) {
            append_symbol(data);
            break;
        }
        if (dncfg_token_is_space(data)) {
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_comma(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_eq(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_backslash(data)) {
            commit(data);
            ctx->state = DNCFG_TOKEN_NEXTLINE;
            break;
        }
        if (dncfg_token_is_sharp(data)) {
            commit(data);
            ctx->state = DNCFG_TOKEN_COMMENT;
            break;
        }
        if (dncfg_token_is_close_square_br(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        if (dncfg_token_is_open_square_br(data)) {
            putback(data);
            commit(data);
            ctx->state = DNCFG_TOKEN_SPACE;
            break;
        }
        on_error(data);
        ctx->state = DNCFG_TOKEN_ERROR;
    break;
    }

}
